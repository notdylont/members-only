<nav>
    <a href="/">Home</a>
    <% if(locals.user) { %>
        <a href="/new-msg">Create a new message</a>
        <% if (locals.user && !locals.user.is_member) {%>
            <a href="/membership">Become a member</a>
        <% } %>
        <a href="/logout">Logout</a>
    <% } else{ %>
        <a href="/login">Login</a>
        <a href="/sign-up">Sign up</a>
    <% } %>
</nav>
